package web

// Intentionally empty. Per-list results live at /api/lists/{id}/results.
// This file is a placeholder so the old import path doesn't surprise
// anyone grepping for "handlers_results"; safe to delete entirely.
